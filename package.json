{
  "name": "druks-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && rsync -az --delete dist/ druks:/home/druks-app/site/ && ssh druks chown -R druks-app:druks-app /home/druks-app/site"
  },
  "devDependencies": {
    "@fontsource-variable/geist": "^5.2.9",
    "@fontsource-variable/geist-mono": "^5.2.8",
    "@fontsource-variable/newsreader": "^5.2.10",
    "astro": "6.4.8"
  }
}
